<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/main.css">
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <form action="/organiser/edit/<%= event.event_id %>" method="POST">
    <label for="title">Title:</label>
    <input type="text" id="title" name="title" value="<%= event.title %>" required>
    <br>
    <label for="description">Description:</label>
    <textarea id="description" name="description" required><%= event.description %></textarea>
    <br>
    <label for="event_date">Event Date:</label>
    <input type="date" id="event_date" name="event_date" value="<%= event.event_date %>" required>
    <br>
    <label for="ticket_price">Ticket Price:</label>
    <input type="number" id="ticket_price" name="ticket_price" value="<%= event.ticket_price || 0 %>" step="0.01" required>
    <br>
    <label for="ticket_quantity">Ticket Quantity:</label>
    <input type="number" id="ticket_quantity" name="ticket_quantity" value="<%= event.ticket_quantity || 0 %>" required>
    <br>

    <button type="submit">Save Changes</button>
  </form>

  <a href="/organiser">Back to Organiser Page</a>
</body>
</html>