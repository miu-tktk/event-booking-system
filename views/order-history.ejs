<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/main.css">
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <ul>
    <% if (orders.length > 0) { %>
      <% orders.forEach(order => { %>
        <li>
          <strong>Event:</strong> <%= order.title %> 
          <br>
          <strong>Date:</strong> <%= order.event_date %> 
          <br>
          <strong>Tickets Booked:</strong> <%= order.quantity %> 
          <br>
          <strong>Booked At:</strong> <%= order.booked_at %>
        </li>
        <hr>
      <% }); %>
    <% } else { %>
      <li>No orders found</li>
    <% } %>
  </ul>
  <br>
  <a href="/attendee">Back to Events</a>
</body>
</html>